
extension Item {
    // TODO: this needs to be handled properly
//    mutating func adjustPriority(_ derivedJudgements: [Judgement]) {
//        if let maxPriority = derivedJudgements.map({$0.truthValue.confidence}).filter({!$0.isNaN}).max() {
//            let newPriority = (priority + maxPriority) / 2
//            priority = min(newPriority, 0.9)
//        } else {
//            let newPriority = priority - 0.1
//            priority = max(0, newPriority)
//        }
//    }
}
